/**
 * Text colors scoped to the Learning V2 podcast-style lesson viewer
 * (LessonViewerScreen_v2 and its components) — intentionally separate from
 * the shared COLORS.textDark used across the rest of the app, so tuning
 * these doesn't affect other screens.
 */
export const LESSON_TEXT_DARK = '#292D34';
export const LESSON_TEXT_GREY = '#ADB3BE';
